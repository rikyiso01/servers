services:
  cron:
    build: .
    read_only: true
    init: true
    network_mode: none
    volumes:
      - /var/run:/mount:ro
    restart: unless-stopped
    container_name: cron
    environment:
      TZ: "Europe/Rome"
      DOCKER_HOST: "unix:///mount/docker.sock"
